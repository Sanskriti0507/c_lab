#include<stdio.h>
int main(){
int l=5, b=8;
int area = l*b;
int perimeter = 2*(l+b);
printf("The area of the rectangle with the length %d and breadth %d is %d", l, b, area);
printf("The perimeter of the rectangle with the length %d and breadth %d is %d", l, b, perimeter);
return 0;
}
